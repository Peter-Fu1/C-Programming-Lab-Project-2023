#include <stdio.h> 
int main()
{
    int x,y,z,sum;  //定义x,y,z,sum为整型
        x=1;
        y=2;
        z=3;
        sum=x+y+z;  //计算三个整数的和
        printf ("sum is %d\n", sum);    //输出sum的值
        return 0;
}